rehome:
  build: .
  # links:
  #   - postgres
  # environment:
  #   DATABASE_URL: "postgres://rehome@postgres/rehome"
  cached: true
# postgres:
#   image: postgres:9.6.2-alpine
#   environment:
#     POSTGRES_USER: rehome
#     POSTGRES_DB: rehome
  dockercfg_generator:
    build:
      image: codeship/kotoc-rehome
      path: ./dockercfg-generator
    add_docker: true
    encrypted_env_file: heroku-deployment.env.encrypted
